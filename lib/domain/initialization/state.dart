class SplashState {
  final bool isLoading;
  final bool isReady;
  final bool isAuthenticated;

  SplashState({
    this.isLoading = false,
    this.isReady = false,
    this.isAuthenticated = false,
  });
}
